# Revision Notes

* Prepare Core Java with examples and tradeoffs.
* OOP pillars must be explained practically.
* Collections require implementation choice knowledge.
* `equals()` and `hashCode()` are critical.
* Streams are lazy until terminal operation.
* Exceptions should be handled with clear intent.
* JVM basics include heap, stack, class loading, and GC.

---

# Cheat Sheet

| Area | Focus |
| ---- | ----- |
| OOP | Encapsulation, inheritance, polymorphism, abstraction |
| Collections | List, Set, Map |
| HashMap | hashCode + equals |
| Streams | map, filter, reduce |
| Exceptions | checked vs unchecked |
| JVM | heap, stack, GC |

---

# Interview Questions with Answers

### 1. What is polymorphism?

One reference type can represent different runtime implementations.

### 2. Why override hashCode with equals?

Hash collections need consistent hash and equality behavior.

